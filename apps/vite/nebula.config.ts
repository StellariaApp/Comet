import { defineConfig } from "@stellaria/comet";

export default defineConfig({
  themes: {
    dark: {
      colors: {
        primary: "#ff0000",
      },
    },
    light: {
      colors: {
        primary: "#00ff00",
      },
    },
  },
});
